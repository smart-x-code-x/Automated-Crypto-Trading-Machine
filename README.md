# sodex

Simple trading bot project (no changes made).

## Requirements

- Python 3.9+
- Install dependencies:

```bash
pip install -r requirements.txt
```

## Quick start

Run the main bot:

```bash
python bot.py
```

Or open the dashboard:

```bash
python dashboard.py
```

(See `requirements.txt` for exact dependencies.)
